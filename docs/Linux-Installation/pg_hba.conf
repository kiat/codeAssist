# Allow internal Unix socket connections (for exec into container)
local   all             all                                     scram-sha-256

# Allow trusted access from Docker bridge network (update the subnet below)
host    all             all             172.18.0.0/16           scram-sha-256

# Reject everything else by default (very important!)
host    all             all             0.0.0.0/0               reject

